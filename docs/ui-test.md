# Frontend direction

Do not migrate the MVP frontend yet.

Keep the static cabinet stable, add design tokens, split the large UI file into modules, and migrate only after the core product flow is stable.
